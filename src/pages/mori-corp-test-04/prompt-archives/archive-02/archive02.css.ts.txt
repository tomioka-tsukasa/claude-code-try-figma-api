import { style } from '@vanilla-extract/css'
import designData from '@/lib/figma-library/design-data.json'

export const containerStyles = style({
  backgroundColor: '#f1f1f1',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'flex-start',
  position: 'relative',
  width: '100%',
  minHeight: '100vh',
  isolation: 'isolate',
})

export const sectionStyles = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '100px',
  alignItems: 'flex-start',
  width: '100%',
  paddingLeft: '100px',
  paddingRight: '200px',
  paddingTop: '170px',
  paddingBottom: '170px',
  boxSizing: 'border-box',
})

export const bodyStyles = style({
  display: 'flex',
  gap: '50px',
  alignItems: 'flex-start',
  justifyContent: 'flex-end',
  width: '100%',
})

export const frameStyles = style({
  display: 'flex',
  flexDirection: 'column',
  height: '628px',
  alignItems: 'flex-start',
  width: '371px',
  position: 'relative',
})

export const textStyles = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '50px',
  alignItems: 'flex-end',
  overflow: 'hidden',
  width: '791px',
})

export const imagesStyles = style({
  display: 'flex',
  gap: '50px',
  alignItems: 'flex-start',
  overflow: 'hidden',
  width: '100%',
})

export const buttonColStyles = style({
  display: 'flex',
  flexWrap: 'wrap',
  gap: '50px',
  alignItems: 'flex-start',
  justifyContent: 'flex-end',
  width: '100%',
})

export const bodyDescStyles = style({
  fontFamily: '"A P-OTF Gothic BBB Pr6N", sans-serif',
  fontSize: '16px',
  lineHeight: '32px',
  color: designData.design_tokens.colors.Basic_Dark,
  minWidth: 'max-content',
  width: 'min-content',

  // selectors: {
  //   '& p': {
  //     margin: 0,
  //     marginBottom: 0,
  //   }
  // }
})
