import React from 'react'
import * as styles from './HeadlineMidashi.css'

interface HeadlineMidashiProps {
  children: React.ReactNode
  color?: string
  'data-name'?: string
  'data-node-id'?: string
}

const HeadlineMidashi: React.FC<HeadlineMidashiProps> = ({
  children,
  color,
  'data-name': dataName,
  'data-node-id': dataNodeId,
}) => {
  return (
    <div
      className={styles.container}
      data-name={dataName}
      data-node-id={dataNodeId}
    >
      <h2 className={styles.headline} style={color ? { color } : undefined}>
        {children}
      </h2>
    </div>
  )
}

export default HeadlineMidashi
